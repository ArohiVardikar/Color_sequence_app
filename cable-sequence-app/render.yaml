
services:
  - type: web
    name: cable-sequence-checker
    env: python3
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 app.py"
    envVars:
      - key: PORT
        value: 10000
